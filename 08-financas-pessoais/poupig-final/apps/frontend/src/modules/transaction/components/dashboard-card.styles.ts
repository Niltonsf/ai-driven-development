/**
 * Surface shared by every card of the dashboard: the same dark gradient and inner
 * highlight of `MetricCard`, so indicators, charts and lists read as one set.
 */
export const DASHBOARD_CARD_CLASSES =
  'relative overflow-hidden border border-white/10 bg-linear-to-br from-zinc-900 via-zinc-900/95 to-zinc-800/45 shadow-[inset_0_1px_0_rgba(255,255,255,0.07)]';
