## 1. Sub-agente 1 — Negócio: limites, catálogo e DTOs

- [x] 1.1 Acrescentar a `DEV_DATA_LIMITS` (`modules/dev/src/data-generator/constants/dev-data-limits.constant.ts`) `maxRecurrences: 20`, `maxInstallmentPlans: 20`, `minInstallments: 2` e `maxInstallmentsPlanned: 24`, documentando o porquê de cada teto e trocando o comentário "Part 2 appends…" pela descrição dos dois geradores
- [x] 1.2 Criar `data-generator/catalog/recurrences.catalog.ts` (`as const`, só dados) com moldes `{ name, direction, valueRange, weight, unit, categoryHint }` em pt-BR conforme a tabela do prompt 20 — saídas mensais, semanais (`Diarista`, `Feira da semana`) e anuais (`Domínio do site`, `Seguro do carro`) e as entradas `Salário` e `Aluguel recebido` com dica `null` —, com pesos em que `MONTH` soma mais que `WEEK + YEAR` e `YEAR` soma menos que `WEEK`, e cabeçalho documentando nomes em 2..100, dicas exatas e a regra da subcategoria mais próxima
- [x] 1.3 Criar `data-generator/catalog/installment-plans.catalog.ts` (`as const`) com moldes de saída `{ name, totalRange, installmentsRange, weight, categoryHint }` (`Geladeira`, `Sofá`, `Notebook`, `Curso de inglês`, `IPVA`, `IPTU`, `Passagens de férias`, `Celular novo`, `Tratamento dentário`…), faixas de parcelas dentro de 2..24 e dicas da tabela do prompt 20; exportar os dois catálogos em `catalog/index.ts`
- [x] 1.4 Ler a skill `module-dto` e criar em `data-generator/dto/`: `SeriesDataRequestDTO` (`recurrences`, `installmentPlans`, `months`, `seed?`), `TransactionSeriesBlueprint` (literais `DirectionLiteral`, `'OPEN' | 'CLOSED'`, `recurrence` com `unit: 'WEEK' | 'MONTH' | 'YEAR'`, `interval`, `weekDay?`, `dayOfMonth?`, `month?`, `startDate`, `installments: number | null`, `note: string | null`, `categoryHint: string | null`, `accountIndex`, `creditCardIndex: number | null`, JSDoc da semelhança proposital com a entrada do `SaveTransactionSeries` sem `id`, `userId` e `endDate`), `SeriesDataPlanDTO` (`series`, `seed`, `period`) e `SeriesDataSummaryDTO` (`recurrences`, `installmentPlans`, `occurrences: { settled, pending, skipped, errors: string[] }`, `seed`); exportar em `dto/index.ts`

## 2. Sub-agente 1 — Negócio: planejador e caso de uso

- [x] 2.1 Ler a skill `module-domain-service` e acrescentar ao `DevDataPlanner` o estático puro `planTransactionSeries(recurrences, installmentPlans, period, links, random)`: recorrências `OPEN` por `weightedPick` com valor em duas casas, `startDate` em `[period.from, period.today]`, regra da unidade do molde com `interval: 1` (`MONTH` `dayOfMonth` 1..28; `WEEK` `weekDay` 1..7; `YEAR` `month` 1..12 e `dayOfMonth` 1..28) e `installments: null`
- [x] 2.2 No mesmo método, parcelamentos `CLOSED`/`OUT`/`MONTH` por `weightedPick`: `installments` na faixa do molde, total em centavos na faixa, parcela `Math.round(totalCents / installments) / 100`, `startDate` entre `max(period.from, primeiro dia do mês installments - 1 meses antes de hoje)` e `today`; em toda série, `accountIndex` em `0..accountCount - 1`, cartão só em `OUT` com `creditCardCount > 0` por `chance`, `note: null` e a dica do molde; recorrências antes dos parcelamentos na lista, reaproveitando os helpers privados de data do planejador
- [x] 2.3 Ler a skill `module-use-case` e criar `data-generator/use-case/plan-series-data.use-case.ts` com `PlanSeriesData(seedSource)`: `execute({ request, today, links })` com `months` opcional; validação de formato acumulando `INVALID_DEV_DATA_REQUEST`, `INVALID_DEV_DATA_QUANTITY`, `INVALID_DEV_DATA_PERIOD` e `INVALID_DEV_DATA_SEED` sem repetição; `DEV_DATA_ACCOUNT_REQUIRED` só depois, quando `links.accountCount === 0`; `seed = request.seed ?? seedSource()`; um único `random` na ordem fixa `periodOf` → `planTransactionSeries` (comentário de contrato de reprodutibilidade); `Result<SeriesDataPlanDTO>` sem persistir. Exportar em `use-case/index.ts` e conferir o barril `modules/dev/src/index.ts`

## 3. Sub-agente 1 — Negócio: testes do domínio

- [x] 3.1 Criar `test/data-generator/plan-series-data.use-case.test.ts` com `seedSource` fixo: nada marcado; quantidade acima do teto, negativa, fracionária e `NaN`; período 0, 13 e ausente; semente fracionária; várias regras acumuladas sem repetição; usuário sem conta (e `DEV_DATA_ACCOUNT_REQUIRED` ausente quando há erro de formato); pedido válido com a quantidade exata de séries; mesma semente produzindo o mesmo plano; sem semente usando o `seedSource` e devolvendo-o no plano
- [x] 3.2 Estender `test/data-generator/dev-data-planner.service.test.ts` com semente fixa: quantidades exatas por `kind`; `startDate` entre `from` e `today`; `CLOSED` sempre `OUT` e `MONTH`, `installments` em 2..24 e parcela com no máximo duas casas cujo produto fica na faixa total do molde (tolerância de centavos); `OPEN` com `installments: null`; âncoras só da unidade e `interval: 1`; entradas sem cartão; nenhum cartão quando `creditCardCount` é `0`; posições de conta e cartão dentro das faixas
- [x] 3.3 Estender `test/data-generator/catalog.test.ts`: nomes das séries em 2..100; dicas de saída dentro de `DEFAULT_SUBCATEGORY_NAMES` (lista já copiada) e dicas de entrada `null`; literais de `direction` e `unit` válidos; faixas de parcelas em 2..24 com mínimo ≤ máximo; ao menos uma recorrência de entrada e uma de cada unidade; soma de pesos `MONTH` > `WEEK + YEAR` e `YEAR` < `WEEK`
- [x] 3.4 Conferir o sub-agente 1: `npx turbo run test --filter=@poupig/dev` e o build do pacote verdes, e busca confirmando nenhum `Math.random` e nenhum import de `@poupig/account`, `credit-card`, `category` ou `transaction` em `modules/dev`

## 4. Sub-agente 2 — Backend: ligação e gravador

- [x] 4.1 Em `apps/backend/src/modules/transaction/transaction.module.ts`, acrescentar `ScheduledTransactionPrisma` ao `exports` — nenhuma outra alteração no arquivo nem no módulo `transaction`
- [x] 4.2 Ler a skill `backend-prisma-data` e injetar `TransactionSeriesPrisma` e `ScheduledTransactionPrisma` no `DataGeneratorWriter`; criar a classe privada `OccurrenceSummary` (`settled`, `pending`, `skipped`, `errors` num `Set` em ordem de primeira ocorrência, `toDTO()`)
- [x] 4.3 Implementar `writeSeriesData(userId, plan, links)` — séries: referências em memória a partir de `links`, `createRandom(plan.seed)` separado para o `resolveLinks`, `SaveTransactionSeries` instanciado uma vez e chamado em sequência sem `id` (`Direction[...]`, `SeriesKind[...]`, `recurrence`, `installments`, `accountId ?? ''`), contando no `ItemSummary` de recorrências ou de parcelamentos pelo `kind`, sem gravar ocorrências de série recusada
- [x] 4.4 Implementar a materialização em `writeSeriesData`: para cada série criada, `findTransactionSeriesById.execute(id, userId)` (falha ou `null` conta um ignorado em `occurrences` com o código e segue); `RecurrenceScheduleCalculator.occurrencesBetween(series.startDate, series.recurrence, { from: series.startDate, to: plan.period.to, endDate: series.endDate, installments: series.installments })`; `SaveScheduledTransaction(scheduledTransactionPrisma, scheduledTransactionPrisma.findScheduledTransactionByOccurrence, transactionSeriesPrisma.findTransactionSeriesById, references)` por ocorrência, em sequência, com `id: randomUUID()`, campos da série gravada, `expectedOn` igual à data e `SETTLED` + `settledOn` antes de `plan.period.today` ou `PENDING` + `settledOn: null` a partir dele; devolver o `SeriesDataSummaryDTO` sem capturar exceções lançadas

## 5. Sub-agente 2 — Backend: controller e roteiro

- [x] 5.1 Ler a skill `backend-controller` e acrescentar ao `dev.controller.ts` o `@Post('series')` com `@HttpCode(HttpStatus.OK)` → `SeriesDataSummaryDTO`: `assertEnabled()` primeiro, `@CurrentUser()`, `recurrences`/`installmentPlans` por `toNumber(...) ?? 0`, `months`/`seed` por `toNumber(...)`, `today` UTC, `writer.loadLinks(user.id)`, `new PlanSeriesData(() => randomInt(0, 2 ** 31)).execute({ request, today, links: links.counts })`, `BadRequestException(planResult.errors)` e `writer.writeSeriesData(user.id, plan, links)`; confirmar que `dev.module.ts` não precisa mudar
- [x] 5.2 Acrescentar ao `data-generator.integration.http`, antes da seção final `Recurso desligado`, com um usuário novo: séries sem nenhuma conta (400 `DEV_DATA_ACCOUNT_REQUIRED`); criação de uma conta pelo `POST transactions`; quantidade de séries acima do teto (400); nada marcado (400); só recorrências; só parcelamentos; os dois, conferidos em `GET /statement?from=&to=` do mês atual (`SETTLED` antes de hoje, `PENDING` a partir de hoje), de um mês futuro (ocorrências geradas presentes) e de um mês passado (cada ocorrência uma vez); `POST series` sem token (401); e, na seção final, `POST series` com o recurso desligado (404)
- [x] 5.3 Conferir o sub-agente 2: build do backend verde, testes do backend e do domínio verdes, `npx eslint` **sem `--fix`** nos arquivos criados ou alterados do backend sem erros (não rodar `npm run lint` no backend) e, no backend, só `apps/backend/src/modules/dev` e a linha do `exports` do `transaction.module.ts` alterados

## 6. Sub-agente 3 — Frontend: dados

- [x] 6.1 Acrescentar `generateSeriesData(token, payload)` em `modules/dev/data/data-generator-api.client.ts` (`POST /dev/data-generator/series`), no formato do `generateTransactionData`, com `SeriesDataRequestDTO`/`SeriesDataSummaryDTO` de `@poupig/dev`
- [x] 6.2 Ler a skill `frontend-form-schema` e criar `modules/dev/data/series-data.schema.ts`: `SeriesDataSelection`, `SeriesDataItem`, `SERIES_DATA_ITEM_LIMITS` de `DEV_DATA_LIMITS`, `SERIES_DATA_SELECTION_FIELD`, `createSeriesDataSchema(selection)` com `Text` e refinamentos (item marcado exige inteiro em `1..limite`; `months` em `1..maxMonths`; `seed` vazia ou inteira; ao menos um item marcado) e `SeriesDataFormData` por `v.infer`, importando `parseInteger` de `transaction-data.schema.ts` e sem repetir números
- [x] 6.3 Acrescentar `useGenerateSeriesData()` e `GenerateSeriesDataResult` em `modules/dev/data/use-data-generator.ts`, no formato do `useGenerateTransactionData` (`generate`, `isSubmitting`, `summary`, `error` traduzido), sem `any` e sem `setState` síncrono em `useEffect`

## 7. Sub-agente 3 — Frontend: componente e tela

- [x] 7.1 Criar `modules/dev/components/series-data-generator.component.tsx` com `react-hook-form` + `v.resolver`: `Card` `Séries e parcelas`; seleção em `useState` (marcadas); valores iniciais `5`/`3`, período `3` e semente vazia; `FormSectionLayout` `Séries` (`Recorrências`, `Parcelamentos` com `DataGeneratorQuantityRow`) e `Opções` (período, semente); aviso curto sobre a gravação das ocorrências até o fim do mês (efetivadas antes de hoje, pendentes a partir de hoje) e a exigência de ao menos uma conta; payload com `0` para desmarcado e sem `seed` vazia; botão com estado de envio; erro traduzido mantendo os valores
- [x] 7.2 No mesmo componente, converter o resumo em linhas `Recorrências`, `Parcelamentos` e `Ocorrências` (`requested = settled + pending + skipped`, `created = settled + pending`, `skipped`, `errors`) para o `DataGeneratorResult`, mostrar abaixo dele `N efetivadas · M pendentes` quando há resumo e disparar `toast.success` com o total de séries criadas e de ocorrências gravadas
- [x] 7.3 Em `modules/dev/pages/data-generator.page.tsx`, renderizar `<SeriesDataGenerator />` logo abaixo de `<TransactionDataGenerator />`, atualizando o comentário, sem mudar cabeçalho, aviso geral nem a regra de recurso indisponível
- [x] 7.4 Atualizar os barris `modules/dev/data/index.ts` e `modules/dev/index.ts` com `series-data.schema` e `series-data-generator.component`
- [x] 7.5 Conferir o sub-agente 3: build do frontend verde e `npx eslint` **sem `--fix`** nos arquivos de frontend criados ou alterados sem erros

## 8. Verificação final

- [x] 8.1 `npm run build` na raiz verde
- [x] 8.2 `npm test` com os testes de domínio e backend verdes
- [x] 8.3 `npx eslint` **sem `--fix`** nos arquivos de frontend e backend criados ou alterados, sem erros (os 88 erros do frontend e os 58 erros e 1 aviso do backend pré-existentes ficam como estão)
- [x] 8.4 Conferir por busca nos arquivos novos ou alterados: nenhum `any`, nenhum `setState` síncrono em `useEffect`, nenhum `Math.random`, nenhuma dependência nova em `package.json`, nenhuma gravação de ocorrência fora do `SaveScheduledTransaction`
- [x] 8.5 Conferir com `git status`/`git diff --stat` que o diff desta mudança se limita a `modules/dev`, `apps/backend/src/modules/dev`, `apps/frontend/src/modules/dev` e a linha do `exports` em `apps/backend/src/modules/transaction/transaction.module.ts`; não testar via navegador (teste manual do usuário)
