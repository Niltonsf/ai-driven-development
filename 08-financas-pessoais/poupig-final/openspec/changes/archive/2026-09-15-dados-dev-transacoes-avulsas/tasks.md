## 1. Sub-agente 1 — Negócio: scaffold do módulo `dev`

- [x] 1.1 Ler a skill `config-new-module` e rodar `node .claude/skills/config-new-module/scripts/create-module.mjs dev`, conferindo o namespace `@poupig/dev`, o pacote `modules/dev`, o `apps/backend/src/modules/dev`, o registro do `DevModule` no `app.module.ts`, o `apps/frontend/src/modules/dev`, a rota `app/(private)/dev/page.tsx` e as dependências nos `package.json` das duas apps
- [x] 1.2 Criar a estrutura `modules/dev/src/data-generator` (`constants/`, `catalog/`, `dto/`, `model/`, `use-case/`) e `modules/dev/test/data-generator`, mantendo `getModuleName()` e o teste do scaffold

## 2. Sub-agente 1 — Negócio: limites, erros, catálogo e DTOs

- [x] 2.1 Criar `data-generator/constants/dev-data-limits.constant.ts` (+ `constants/index.ts`) com `DEV_DATA_LIMITS` (`maxAccounts: 10`, `maxCreditCards: 10`, `maxTransactions: 500`, `maxMonths: 12`) `as const`, documentando que os tetos mantêm a execução síncrona e o banco de desenvolvimento utilizável e que a parte 2 acrescenta os seus
- [x] 2.2 Criar `data-generator/dev-data.errors.ts` com `DevDataErrors` `as const` (`INVALID_DEV_DATA_REQUEST`, `INVALID_DEV_DATA_QUANTITY`, `INVALID_DEV_DATA_PERIOD`, `INVALID_DEV_DATA_SEED`, `DEV_DATA_ACCOUNT_REQUIRED`, `DEV_DATA_DISABLED`), no desenho do `movement.errors.ts`
- [x] 2.3 Criar `data-generator/catalog/accounts.catalog.ts` com ao menos 10 contas em pt-BR (`name`, `type` literal de `AccountType`, `financialInstitution`, `icon` do `lucide-react`, `color` `#RRGGBB`), só dados, `as const`, com cabeçalho documentando 2..100 caracteres e que o módulo não importa outro módulo de domínio
- [x] 2.4 Criar `data-generator/catalog/credit-cards.catalog.ts` com ao menos 10 cartões (`name`, `brand` literal de `CardBrand`, `color`, `limitRange` em reais, pares plausíveis `closingDay`/`dueDay`), `as const`
- [x] 2.5 Criar `data-generator/catalog/one-off.catalog.ts` com moldes de saída (`Revisão do carro`, `Pagamento do seguro`, `Mercado`, `Farmácia`, `Restaurante`, `Uber`, `Presente de aniversário`, `Padaria`, `Combustível`…) e de entrada (`Freelance`, `Venda de item usado`, `Reembolso`), cada um com `direction`, `valueRange` em reais, `weight` (frequentes com peso maior) e `categoryHint` com nome exato de subcategoria padrão (saída) ou `null` (entrada), documentando a regra das dicas; criar `catalog/index.ts`
- [x] 2.6 Ler a skill `module-dto` e criar em `data-generator/dto/`: `DevDataPeriodDTO` (`from`, `to`, `today`), `DevDataItemSummaryDTO` (`requested`, `created`, `skipped`, `errors: string[]`), `TransactionDataRequestDTO` (`accounts`, `creditCards`, `transactions`, `months`, `seed?`), `AccountBlueprint`, `CreditCardBlueprint`, `TransactionBlueprint` (sem `id`/`userId`, enums como literais, `accountIndex`, `creditCardIndex: number | null`, `categoryHint: string | null`, `note: string | null`, JSDoc explicando a semelhança proposital com as entradas dos casos de uso), `TransactionDataPlanDTO` (`accounts`, `creditCards`, `transactions`, `seed`, `period`), `TransactionDataSummaryDTO` (`accounts`, `creditCards`, `transactions`, `seed`) e `DevDataLinksDTO` (`accountCount`, `creditCardCount`), com `dto/index.ts`

## 3. Sub-agente 1 — Negócio: gerador, planejador e caso de uso

- [x] 3.1 Criar `data-generator/model/pseudo-random.ts` com `createRandom(seed)` (`mulberry32`, semente normalizada com `>>> 0`) expondo `next`, `int(min, max)` inclusivo, `pick`, `weightedPick(list, weight)`, `chance(probability)` e `shuffle` sem mutar a entrada, e o tipo `SeedSource = () => number`; documentar que a semente torna a execução reprodutível e que `Math.random()` não aparece no módulo
- [x] 3.2 Ler a skill `module-domain-service` e criar `data-generator/model/dev-data-planner.service.ts` com `DevDataPlanner` e métodos estáticos puros: `periodOf(months, today)` e `monthsOf(period)` em UTC sobre `YYYY-MM-DD` com padding manual
- [x] 3.3 Implementar `DevDataPlanner.planAccounts(quantity, random)` e `planCreditCards(quantity, random)`: `shuffle` do catálogo + `slice` (sem repetição), `lastFourDigits` com 4 dígitos, `limit` em reais convertido para centavos inteiros e par de dias sorteado
- [x] 3.4 Implementar `DevDataPlanner.planTransactions(quantity, period, links, random)`: `Math.floor(quantity * 0.15)` entradas embaralhadas entre as saídas; mês da transação `i` por `i % months` começando pelo mês atual; dia sorteado no mês; molde por `weightedPick` da direção; valor com duas casas na faixa; `accountIndex` entre as contas disponíveis; cartão só em `OUT` quando há cartão, por `chance`; `SETTLED` com `settledOn = expectedOn` antes de `today` e `PENDING` com `settledOn: null` a partir de `today`; `note: null`; ordenação estável por `expectedOn`
- [x] 3.5 Ler a skill `module-use-case` e criar `data-generator/use-case/plan-transaction-data.use-case.ts` com `PlanTransactionData(seedSource)`: validação de formato acumulando códigos sem repetição, `DEV_DATA_ACCOUNT_REQUIRED` só depois, semente `request.seed ?? seedSource()`, um único `random` consumido na ordem fixa `periodOf` → `planAccounts` → `planCreditCards` → `planTransactions` (com contagens existentes somadas às planejadas, e comentário de que a ordem é contrato de reprodutibilidade), devolvendo `Result<TransactionDataPlanDTO>` sem persistir
- [x] 3.6 Criar os barris `data-generator/model/index.ts`, `use-case/index.ts`, `data-generator/index.ts` e exportar tudo em `modules/dev/src/index.ts`

## 4. Sub-agente 1 — Negócio: testes do domínio

- [x] 4.1 Criar `test/data-generator/pseudo-random.test.ts`: mesma semente gera a mesma sequência; `int` respeita os limites inclusivos; `weightedPick` nunca escolhe peso zero; `shuffle` não muta a entrada
- [x] 4.2 Criar `test/data-generator/dev-data-planner.service.test.ts`: `periodOf` com 1 e 12 meses, virada de ano e fevereiro bissexto; datas sempre dentro do período e todos os meses cobertos quando `quantity >= months`; mês atual coberto com 1 transação e 12 meses; `SETTLED`/`settledOn` antes de `today` e `PENDING` sem `settledOn` a partir de `today`; entradas nunca com cartão e sem cartão quando não há cartões; no máximo 20% de entradas; posições de conta dentro das disponíveis; 10 contas e 10 cartões sem nome repetido; limite de cartão em centavos inteiros dentro da faixa; `lastFourDigits` com 4 dígitos
- [x] 4.3 Criar `test/data-generator/catalog.test.ts`: todos os nomes com 2..100 caracteres; catálogos de contas e cartões com ao menos o teto de entradas; literais de `type`, `brand` e `direction` dentro dos conjuntos esperados; toda dica de saída dentro da lista de subcategorias padrão **copiada no teste** e toda dica de entrada `null`; `Mercado` com peso maior que `Revisão do carro`
- [x] 4.4 Criar `test/data-generator/plan-transaction-data.use-case.test.ts` com `seedSource` fixo: pedido vazio; quantidade acima do teto, negativa, fracionária e `NaN`; período 0, 13 e ausente; semente fracionária; várias regras acumuladas sem repetição; transação sem conta existente nem a criar; transação sem conta existente com contas a criar aceita; mesma semente produzindo o mesmo plano; sem semente usando o `seedSource` e devolvendo a semente no plano
- [x] 4.5 Conferir o sub-agente 1: `npx turbo run test --filter=@poupig/dev` e o build do pacote verdes, e busca confirmando nenhum `Math.random` e nenhum import de `@poupig/account`, `credit-card`, `category` ou `transaction` em `modules/dev`

## 5. Sub-agente 2 — Backend: ambiente, limpeza e composição

- [x] 5.1 Acrescentar `DEV_TOOLS_ENABLED="true"` em `apps/backend/.env.example` (e no `.env` local), com comentário de que produção não define a variável
- [x] 5.2 Remover `apps/backend/src/modules/dev/dev.prisma.ts` e `apps/backend/prisma/models/dev.model.prisma`, conferindo que nenhuma migração é gerada e que o client Prisma continua compilando
- [x] 5.3 Criar `apps/backend/src/modules/dev/dev.config.ts` com `DevConfig` (`@Injectable()`) lendo `DEV_TOOLS_ENABLED` pelo `ConfigService` e expondo `isEnabled` (`=== 'true'`)
- [x] 5.4 Criar `apps/backend/src/modules/dev/in-memory-movement-references.ts` com `InMemoryMovementReferences implements MovementReferencesQuery` sobre três `Set` e os métodos `replaceAccounts`, `replaceCreditCards` e `setSubcategories`, sem tocar no módulo `transaction`
- [x] 5.5 Ler a skill `backend-prisma-data` e criar `apps/backend/src/modules/dev/data-generator.writer.ts` com `DataGeneratorWriter` (`@Injectable()`) injetando `AccountPrisma`, `CreditCardPrisma`, `CategoryPrisma` e `TransactionPrisma`; implementar `loadLinks(userId)` paginando contas e cartões (página de 100 até o `total`, só ativos) e guardando subcategorias ativas de categorias ativas
- [x] 5.6 Implementar `resolveLinks(blueprint, links, random)`: conta e cartão por posição com módulo sobre a lista, subcategoria por nome normalizado (sem acento e sem caixa), sorteio pelo `random` separado quando a dica não casa, `null` para dica nula ou usuário sem subcategoria
- [x] 5.7 Implementar `writeTransactionData(userId, plan, links)` sequencial: contas por `new SaveAccount(accountPrisma)` com `randomUUID()` e uma nova tentativa com o menor sufixo `n ≥ 2` livre em `ACCOUNT_NAME_ALREADY_EXISTS`; cartões por `new SaveCreditCard(creditCardPrisma)` com a mesma regra em `CREDIT_CARD_NAME_ALREADY_EXISTS`; recarga de contas e cartões e atualização das referências em memória; transações por `new SaveTransaction(transactionPrisma, references)` sem `id`; acumulando o `TransactionDataSummaryDTO` com `errors` sem repetição e sem capturar exceções lançadas

## 6. Sub-agente 2 — Backend: controller, módulo e roteiro

- [x] 6.1 Ler a skill `backend-controller` e reescrever `dev.controller.ts` como `@Controller('dev/data-generator')`: checagem do ambiente como primeira linha de cada método (`NotFoundException([DevDataErrors.DEV_DATA_DISABLED])`), `GET status` → `{ enabled: true }` e `POST transactions` com `@HttpCode(HttpStatus.OK)`, dono por `@CurrentUser()`, corpo sem `userId`
- [x] 6.2 No `POST transactions`, converter `accounts`, `creditCards`, `transactions`, `months` e `seed` por helper privado (`undefined`/`null`/`''` → ausente; string por `Number(trim)`, texto virando `NaN` para a validação do domínio), quantidades ausentes como `0`, `today` UTC em `YYYY-MM-DD`, `loadLinks` → `new PlanTransactionData(() => randomInt(0, 2 ** 31)).execute(...)` → `BadRequestException(codes)` em falha → `writeTransactionData`
- [x] 6.3 Reescrever `dev.module.ts` importando `AccountModule`, `CreditCardModule`, `CategoryModule` e `TransactionModule`, registrando `DevController`, `DataGeneratorWriter` e `DevConfig`, sem `DbModule` direto; ajustar `index.ts`
- [x] 6.4 Criar `data-generator.integration.http` (Rest Client) cobrindo: `GET status` ligado e desligado (404); execução só de contas; execução completa conferida em `GET /transactions` do mês atual (`SETTLED` antes de hoje, `PENDING` a partir de hoje); transações sem conta (400); quantidade acima do teto (400); nada marcado (400); duas execuções com a mesma `seed` (sufixo na segunda); sem token (401)
- [x] 6.5 Conferir o sub-agente 2: build do backend verde, testes do backend e do domínio verdes, `npx eslint` **sem `--fix`** nos arquivos criados ou alterados do backend sem erros (não rodar `npm run lint` no backend) e nenhum arquivo de outro módulo do backend alterado

## 7. Sub-agente 3 — Frontend: ambiente, dados e mensagens

- [x] 7.1 Consultar `node_modules/next/dist/docs/` sobre variáveis `NEXT_PUBLIC_*` e layouts do App Router antes de mexer em rota, layout ou ambiente
- [x] 7.2 Acrescentar `!.env.example` ao `apps/frontend/.gitignore` logo depois de `.env*`; revisar que `apps/frontend/.env.example` não tem segredo e acrescentar nele (e no `.env.local`) `NEXT_PUBLIC_DEV_TOOLS_ENABLED=true` com comentário de que o valor é embutido no build e de que produção não define a variável
- [x] 7.3 Criar `modules/dev/data/dev-tools.env.ts` com `DEV_TOOLS_ENABLED = process.env.NEXT_PUBLIC_DEV_TOOLS_ENABLED === 'true'`
- [x] 7.4 Criar `modules/dev/data/data-generator-api.client.ts` no formato dos outros clientes: `DataGeneratorApiError` (com `status` e códigos), `fetchDataGeneratorStatus(token)` e `generateTransactionData(token, payload)`, DTOs de `@poupig/dev`
- [x] 7.5 Acrescentar `INVALID_DEV_DATA_REQUEST`, `INVALID_DEV_DATA_QUANTITY`, `INVALID_DEV_DATA_PERIOD`, `INVALID_DEV_DATA_SEED`, `DEV_DATA_ACCOUNT_REQUIRED` e `DEV_DATA_DISABLED` em `shared/i18n/messages.pt.ts` e `messages.en.ts`, e conferir que `ACCOUNT_NAME_ALREADY_EXISTS`, `CREDIT_CARD_NAME_ALREADY_EXISTS` e os códigos `MOVEMENT_*`/`TRANSACTION_*` já têm tradução nos dois arquivos
- [x] 7.6 Ler a skill `frontend-form-schema` e criar `modules/dev/data/transaction-data.schema.ts` com `createTransactionDataSchema(selection)`: quantidades validadas contra `DEV_DATA_LIMITS` só para itens marcados, `months` em `1..maxMonths`, `seed` vazia ou inteira, refinamento exigindo ao menos um item marcado, tipos por `v.infer`, sem repetir números
- [x] 7.7 Criar `modules/dev/data/use-data-generator.ts`: `useDataGeneratorStatus()` (`'loading' | 'enabled' | 'disabled' | 'error'`, `'disabled'` calculado sem requisição quando a chave está desligada, `404` → `'disabled'`, estado gravado só no retorno da promessa com cancelamento na limpeza) e `useGenerateTransactionData()` (`generate`, `isSubmitting`, `summary`, `error` por `getErrorMessage`), sem `any` e sem `setState` síncrono em `useEffect`

## 8. Sub-agente 3 — Frontend: componentes, tela e menu

- [x] 8.1 Criar `components/data-generator-quantity-row.component.tsx`: `Checkbox` + `Input` numérico desabilitado quando desmarcado + teto exibido ao lado, reaproveitável pela parte 2
- [x] 8.2 Criar `components/data-generator-result.component.tsx`: `EmptyListState` antes da primeira execução; tabela com uma linha por item (`rótulo`, `solicitados`, `criados`, `ignorados`), `Badge` com a semente e lista de códigos de erro traduzidos e sem repetição quando houver
- [x] 8.3 Criar `components/transaction-data-generator.component.tsx` com `react-hook-form` + `v.resolver`: `Card` `Transações avulsas`, `FormSectionLayout` `Cadastros de apoio` (Contas, Cartões de crédito), `Transações` (Transações avulsas) e `Opções` (Período, Semente), caixas em `useState` (marcadas), valores iniciais 3/2/150 e período 3, payload com `0` para desmarcado e sem `seed` vazia, botão com estado de envio, `DataGeneratorResult` abaixo, `toast` com o total criado e erro traduzido mantendo os valores
- [x] 8.4 Criar `pages/data-generator.page.tsx`: indisponível (`EmptyListState`) quando `DEV_TOOLS_ENABLED` é falso ou o status é `'disabled'`; carregamento; erro traduzido; e, ligado, `PageSectionHeader` (badge `Desenvolvimento`, título `Gerador de Massa de Dados`), aviso sobre conta logada, efetivação dos dias passados e categorias padrão, e a lista de geradores com o `TransactionDataGenerator`; sem guard na página
- [x] 8.5 Remover `components/dev-dashboard.component.tsx` e `pages/dashboard.page.tsx` e fazer `app/(private)/dev/page.tsx` renderizar a `DataGeneratorPage`
- [x] 8.6 Em `app/(private)/layout.tsx`, remover da primeira seção o item anexado pela skill (e o import de ícone sem uso) e acrescentar no fim do `NAVIGATION_SECTIONS` `...(DEV_TOOLS_ENABLED ? [EXTRAS_SECTION] : [])` com `EXTRAS_SECTION` (`extras`, `Extras`, item `dev`/`Desenvolvimento`/`/dev`/`CodeXml`/`prefix`), comentário de que novas seções entram antes dela e nenhum outro `process.env` no arquivo
- [x] 8.7 Atualizar os barris `modules/dev/data/index.ts` e `modules/dev/index.ts` e buscar referências a `DevDashboard`/`dashboard.page` do scaffold, confirmando que não sobrou nenhuma
- [x] 8.8 Conferir o sub-agente 3: build do frontend verde e `npx eslint` **sem `--fix`** nos arquivos de frontend criados ou alterados sem erros

## 9. Verificação final

- [x] 9.1 `npm run build` na raiz verde
- [x] 9.2 `npm test` com os testes de domínio e backend verdes
- [x] 9.3 `npx eslint` **sem `--fix`** nos arquivos de frontend e backend criados ou alterados, sem erros (os 88 e os 58 pré-existentes ficam como estão)
- [x] 9.4 Conferir por busca nos arquivos novos ou alterados: nenhum `any`, nenhum `setState` síncrono em `useEffect`, nenhum `Math.random`, nenhuma dependência nova além de `@poupig/dev`, e `process.env.NEXT_PUBLIC_DEV_TOOLS_ENABLED` só em `dev-tools.env.ts`
- [x] 9.5 Conferir com `git status`/`git diff --stat` que o diff se limita a `modules/dev`, `apps/backend/src/modules/dev`, `app.module.ts`, os `package.json` das apps e o `package-lock.json`, `apps/frontend/src/modules/dev`, `app/(private)/dev/page.tsx`, a seção `extras` do `layout.tsx`, `shared/i18n`, os dois `.env.example`, `apps/frontend/.gitignore` e a remoção do `dev.model.prisma`; não testar via navegador (teste manual do usuário)
