/**
 * Re-export do novo `Topbar` portado fielmente do template demo.tailadmin.com.
 * Mantido por compatibilidade com importações pré-existentes.
 */
export { Topbar as TopBar } from "./topbar/topbar.component";
