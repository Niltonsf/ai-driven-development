model Product {
  id              String    @id
  name            String
  description     String?
  price           Decimal   @db.Decimal(12, 2)
  status          String
  availableOnline Boolean   @default(false)
  featured        Boolean   @default(false)
  allowsPreOrder  Boolean   @default(false)
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
  deletedAt       DateTime?

  @@map("products")
}
