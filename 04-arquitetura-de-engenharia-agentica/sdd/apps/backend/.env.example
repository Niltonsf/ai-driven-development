DB_HOST="localhost"
DB_PORT="6321"
DB_USER="sdd_sdd"
DB_PASSWORD="docker"
DB_NAME="sdd_sdd_db"
DATABASE_URL="postgresql://sdd_sdd:docker@localhost:6321/sdd_sdd_db?schema=public"
PORT="4000"
# Substitua por um segredo robusto (ex.: `openssl rand -hex 64`) antes de subir para producao
JWT_SECRET="change-me-in-production-use-a-long-random-string"
JWT_EXPIRES_IN="14d"
