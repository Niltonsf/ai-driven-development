Domain driven development + Clean architecture

- modulos de negocio fica na pasta `modules/`, tambem criamos pasta `modules` dentro de `backend/`, `web/`, `module/`
- infraestrutura (serve para qualquer projeto) fica em `packages/` é a camada de negocio
