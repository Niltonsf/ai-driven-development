/openspec-propose

criar um nova spec para o cadastro de cartão dentro do módulo apps/backend/src/modules/cartao usando como base a spec openspec/changes/archive/2026-06-18-spec-modulo-backend-conta.

O foco dessa spec é o mesmo da spec de referência, ou seja, apenas o módulo de backend. Essa spec só será executada depois que a spec de negócio tenha sido concluída.
