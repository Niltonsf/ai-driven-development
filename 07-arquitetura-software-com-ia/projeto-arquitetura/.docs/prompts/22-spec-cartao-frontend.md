/openspec-propose

criar um nova spec para o cadastro de cartão dentro do módulo apps/web/src/modules/cartao usando como base a spec openspec/changes/archive/2026-06-18-spec-modulo-frontend-conta.

O foco dessa spec é o mesmo da spec de referência, ou seja, apenas o módulo de frontend. Essa spec só será executada depois que a spec de negócio e backend tenham sido concluídas.
