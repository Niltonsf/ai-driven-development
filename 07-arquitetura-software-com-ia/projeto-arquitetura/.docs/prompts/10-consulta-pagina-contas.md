Criar a interface/implementação da consulta paginada usando a skill /module-query-cqrs e o DTO packages/shared/src/query/pagination.dto.ts. A consulta deve retornar as contas (DTOs) de forma paginada.

Quero os seguintes passos seja executados:

1. Criar a interface na pasta modules/contas/src/conta/provider com nome contas.query.ts
2. Implementar em apps/backend/src/modules/contas/contas.prisma.ts a interface criada no passo anterior, seguindo o padrão de criar uma constante com implementação inline (objeto literal).
